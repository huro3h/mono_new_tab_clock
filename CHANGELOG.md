# Changelog

このプロジェクトの主な変更点を記録します。

フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョニングは [セマンティック バージョニング](https://semver.org/lang/ja/) を採用しています。

新しいバージョンを上に追記します。未リリースの変更は `[Unreleased]` にためていき、
リリース時にバージョン番号と日付へ繰り上げます。

## [Unreleased]

## [1.0.0] - 2026-09-13

### Added
- 初回リリース。以下の機能を含みます。
- **新しいタブを時計に置き換え**: 24時間表記の `HH:MM`（秒なし）。毎秒更新するので分の切り替わりが即座に反映される。
- **日付表示**: `Tuesday, July 14` のような英語表記。
- **コロンの真円描画**: フォントの文字ではなく CSS の円2つで描くため、どのフォントでも丸く表示され、サイズと透明度に自動追従する。
- **設定パネル**: 既定では非表示。左下の歯車アイコン、またはキーボードショートカットで開閉できる（初期状態では未割り当て）。
- **カスタマイズ**: 背景の明るさ（グレースケール）、文字サイズ、透明度、フォント（System / Montserrat / Josefin Sans / Roboto）を変更できる。設定はすべて `localStorage` に保存される。
- **時計の位置移動**: ドラッグして好きな位置に置ける。ビューポート比率で保存するのでウィンドウをリサイズしてもずれにくい。
- **リセットとバックアップ**: 既定値へのリセットと、設定の JSON エクスポート / インポート。
- **オフライン動作**: フォントを同梱して `@font-face` で読み込むため、外部への通信が一切ない。
- **権限リクエストなし**: すべてローカルで完結する。
- **拡張機能アイコン**: グレースケールの時計アイコン（16/32/48/128px）。
